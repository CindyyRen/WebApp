namespace WebApp.Database;

// 定义 schema 常量
public static class Schemas
{
    //const = 隐式 static
    //const 之外的其他字段、属性、方法，如果你希望它是类级别共享的（静态成员），就必须加 static。
    public const string Application = "dev_habit";
}
